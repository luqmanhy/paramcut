# paramcut ✂️

`paramcut` is a fast, lightweight CLI tool written in Go designed to strip unwanted query parameters from URLs. Whether you're cleaning up tracking tokens (UTM/ITM) for a report or sanitizing lists of URLs for security testing, `paramcut` handles it with ease.